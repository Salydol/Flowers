# Запуск приложения в IntelliJ IDEA

## Способ 1: Автоматический (рекомендуется)

1. Откройте проект в IntelliJ IDEA
2. Дождитесь индексации и загрузки Maven зависимостей
3. Найдите файл `src/main/java/App.java`
4. Правой кнопкой мыши на `App.java` → **Run 'App.main()'**
   - Или нажмите `Shift + F10`
   - Или нажмите зеленую стрелку слева от метода `main()`

## Способ 2: Через конфигурацию запуска

1. В верхней панели IntelliJ найдите выпадающий список конфигураций (рядом с кнопками Run/Debug)
2. Выберите **"App"** из списка
3. Нажмите зеленую кнопку **Run** (▶) или `Shift + F10`

## Если приложение не запускается

### Ошибка: "Error: JavaFX runtime components are missing"

**Решение для Java 11+ (модульная система):**

1. Откройте **Run → Edit Configurations...**
2. Выберите конфигурацию **App**
3. В поле **VM options** добавьте:
   ```
   --module-path "путь_к_javafx/lib" --add-modules javafx.controls,javafx.fxml
   ```
   
   Или используйте переменную окружения:
   ```
   --module-path "%PATH_TO_FX%" --add-modules javafx.controls,javafx.fxml
   ```

4. Нажмите **OK**

**Альтернативное решение (через Maven):**

IntelliJ IDEA автоматически использует JavaFX из Maven зависимостей, но иногда нужно:

1. **File → Project Structure → Modules**
2. Убедитесь, что модуль **flowerapps** выбран
3. На вкладке **Dependencies** должны быть видны:
   - `javafx-controls-17.0.2.jar`
   - `javafx-fxml-17.0.2.jar`
   - `postgresql-42.6.0.jar`

4. Если их нет, выполните:
   - **View → Tool Windows → Maven**
   - Нажмите кнопку **Reload All Maven Projects** (🔄)

### Ошибка подключения к базе данных

1. Убедитесь, что PostgreSQL запущен
2. Проверьте настройки в `src/main/java/database/DatabaseConnection.java`:
   - URL: `jdbc:postgresql://localhost:5432/flower_shop`
   - USER: `postgres`
   - PASSWORD: ваш пароль

3. Убедитесь, что база данных создана:
   ```sql
   CREATE DATABASE flower_shop;
   ```

4. Выполните `database_schema.sql` для создания таблиц

## Быстрый запуск

После первой настройки просто:
- **Shift + F10** - запуск
- **Shift + F9** - отладка

## Проверка настроек

1. **File → Project Structure → Project**
   - Project SDK: Java 17 или выше
   - Project language level: 17

2. **File → Settings → Build, Execution, Deployment → Build Tools → Maven**
   - Убедитесь, что Maven правильно настроен

3. **View → Tool Windows → Maven**
   - Нажмите **Reload All Maven Projects** (🔄)

