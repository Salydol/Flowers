# Flower Shop - Система управления цветочным магазином

## 📋 Возможности системы

✅ **Создание букетов**
- Выбор цветов с указанием количества
- Добавление упаковки
- Выбор декораций
- Автоматический подсчет стоимости
- Сохранение букетов в базу данных

✅ **Управление каталогом**
- Добавление/редактирование/удаление цветов
- Добавление/редактирование/удаление упаковки
- Добавление/редактирование/удаление декораций
- Все изменения автоматически отображаются в системе

✅ **Просмотр сохраненных букетов**
- Список всех созданных букетов
- Детальная информация по каждому букету
- История создания

## 🗄️ Настройка базы данных

### 1. Установка PostgreSQL

Скачайте и установите PostgreSQL с официального сайта: https://www.postgresql.org/download/

### 2. Создание базы данных

Откройте pgAdmin или командную строку PostgreSQL и выполните:

```sql
CREATE DATABASE flower_shop;
```

### 3. Создание таблиц

Подключитесь к базе данных `flower_shop` и выполните SQL скрипт из файла `database_schema.sql`

### 4. Настройка подключения

Откройте файл `DatabaseConnection.java` и измените параметры подключения:

```java
private static final String URL = "jdbc:postgresql://localhost:5432/flower_shop";
private static final String USER = "postgres";
private static final String PASSWORD = "ваш_пароль"; // Замените на свой пароль
```

## 📦 Установка зависимостей

Добавьте в `pom.xml` (если используете Maven):

```xml
<dependencies>
    <!-- JavaFX -->
    <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-controls</artifactId>
        <version>17.0.2</version>
    </dependency>
    
    <!-- PostgreSQL JDBC Driver -->
    <dependency>
        <groupId>org.postgresql</groupId>
        <artifactId>postgresql</artifactId>
        <version>42.6.0</version>
    </dependency>
</dependencies>
```

Или скачайте JDBC драйвер вручную:
- PostgreSQL JDBC: https://jdbc.postgresql.org/download/

## 📁 Структура проекта

```
flower-shop/
├── src/
│   ├── app.java                      # Главный класс приложения
│   ├── Model/
│   │   ├── flower.java               # Модель цветка
│   │   ├── packaging.java            # Модель упаковки
│   │   ├── decoration.java           # Модель декорации
│   │   └── bouquetItem.java          # Элемент букета
│   ├── database/
│   │   ├── DatabaseConnection.java   # Подключение к БД
│   │   └── dao/
│   │       ├── FlowerDAO.java        # DAO для цветов
│   │       ├── PackagingDAO.java     # DAO для упаковки
│   │       ├── DecorationDAO.java    # DAO для декораций
│   │       └── BouquetDAO.java       # DAO для букетов
│   ├── service/
│   │   └── BouquetCalculator.java    # Расчет стоимости
│   └── UI/
│       ├── MainWindow.java           # Главное окно
│       └── views/
│           ├── CreateBouquetView.java      # Создание букета
│           ├── FlowersCatalogView.java     # Каталог цветов
│           ├── PackagingView.java          # Каталог упаковки
│           ├── DecorationView.java         # Каталог декораций
│           └── SavedBouquetsView.java      # Сохраненные букеты
└── database_schema.sql               # SQL скрипт для создания БД
```

## 🚀 Запуск приложения

1. Убедитесь, что PostgreSQL запущен
2. База данных создана и заполнена начальными данными
3. В файле `DatabaseConnection.java` указаны правильные параметры подключения
4. Запустите класс `app.java`

## 💡 Использование

### Создание букета:
1. Перейдите в раздел "Создать букет"
2. Выберите цветы и укажите количество
3. Добавьте упаковку (необязательно)
4. Выберите декорации (необязательно)
5. Нажмите "Сохранить букет"

### Управление каталогом:
1. Перейдите в нужный раздел (Цветы/Упаковка/Декор)
2. Нажмите "+ Добавить" для создания нового элемента
3. Используйте кнопки ✏️ и 🗑️ для редактирования и удаления

### Просмотр букетов:
1. Перейдите в раздел "Мои букеты"
2. Нажмите "Подробнее" для просмотра деталей букета

## 🔧 Устранение неполадок

**Ошибка подключения к БД:**
- Проверьте, что PostgreSQL запущен
- Убедитесь, что параметры подключения верны
- Проверьте, что база данных `flower_shop` существует

**JDBC драйвер не найден:**
- Добавьте postgresql-XX.jar в classpath проекта
- Или добавьте зависимость в Maven/Gradle

**JavaFX не найден:**
- Добавьте JavaFX в путь к библиотекам проекта
- Или используйте Maven для автоматической загрузки

## 📊 База данных

### Таблицы:

- **flowers** - цветы (id, name, price, image_path)
- **packaging** - упаковка (id, name, price, image_path)
- **decorations** - декорации (id, name, price, image_path)
- **bouquets** - букеты (id, name, total_price, packaging_id, created_at)
- **bouquet_flowers** - цветы в букете (id, bouquet_id, flower_id, quantity, price_at_moment)
- **bouquet_decorations** - декорации в букете (id, bouquet_id, decoration_id, price_at_moment)

## 🎨 Особенности реализации

- **Транзакции**: Сохранение букета выполняется в одной транзакции
- **Кэширование цен**: При сохранении букета цены фиксируются (price_at_moment)
- **Каскадное удаление**: При удалении букета автоматически удаляются связанные записи
- **Автоматическое обновление**: Изменения в каталогах сразу доступны при создании букетов

## 👨‍💻 Разработка

Проект использует:
- Java 11+
- JavaFX для UI
- SQLite для базы данных
- JDBC для работы с БД
- DAO паттерн для работы с данными